describe('Deve calcular a soma de duas variáveis', ()=>{

   it('I need correct in sum ', ()=> {
     const total = 11
     const a = 5
     const b = 6
     let sum = a + b
     expect(sum).toBe(total)
   })

   it('')

})